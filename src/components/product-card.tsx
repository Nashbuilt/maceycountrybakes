import Image from"next/image";import Link from"next/link";import{Product}from"@/lib/types";
export function ProductCard({product}:{product:Product}){return<article className="group"><Link href={`/shop/${product.slug}`} className="block overflow-hidden rounded-[1.5rem] bg-oat">{product.image_url&&<Image src={product.image_url} alt="" width={800} height={600} className="aspect-[4/3] w-full object-cover transition duration-500 group-hover:scale-[1.03]" sizes="(max-width: 768px) 100vw, 33vw"/>}</Link><div className="flex items-start justify-between gap-4 pt-4"><div><p className="eyebrow">{product.category}</p><h3 className="mt-1 font-serif text-2xl"><Link href={`/shop/${product.slug}`}>{product.name}</Link></h3><p className="mt-2 text-sm text-cocoa/65">{product.short_description}</p></div><p className="whitespace-nowrap font-semibold">£{(product.price_pence/100).toFixed(2)}</p></div></article>}
