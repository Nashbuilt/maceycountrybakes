import{getProducts}from"@/lib/data";import{ProductCard}from"@/components/product-card";
export const metadata={title:"Shop"};
export default async function Shop(){const products=await getProducts(),cats=[...new Set(products.map(p=>p.category))];return<section className="shell py-16"><p className="eyebrow">Made for this week</p><h1 className="mt-3 font-serif text-6xl">The bakery counter</h1><p className="mt-5 max-w-2xl text-cocoa/65">Everything is made in small batches. Choose your collection time in the basket; availability is checked again before payment.</p>{cats.map(c=><section key={c} className="pt-16"><div className="mb-7 flex items-center gap-5"><h2 className="font-serif text-4xl">{c}</h2><div className="h-px flex-1 bg-cocoa/15"/></div><div className="grid gap-10 sm:grid-cols-2 lg:grid-cols-3">{products.filter(p=>p.category===c).map(p=><ProductCard key={p.id} product={p}/>)}</div></section>)}</section>}
