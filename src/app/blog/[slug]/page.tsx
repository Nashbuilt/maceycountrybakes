import{notFound}from"next/navigation";import{createSupabaseServerClient}from"@/lib/supabase/server";
export default async function Post({params}:{params:Promise<{slug:string}>}){const{slug}=await params,s=await createSupabaseServerClient();if(!s)notFound();const{data:p}=await s.from("blog_posts").select("*").eq("slug",slug).eq("is_published",true).single();if(!p)notFound();return<article className="shell max-w-3xl py-16"><p className="eyebrow">Bakery journal</p><h1 className="mt-4 font-serif text-6xl">{p.title}</h1><p className="mt-4 text-cocoa/60">{new Date(p.published_at).toLocaleDateString("en-GB",{dateStyle:"long"})}</p><div className="prose mt-10 whitespace-pre-wrap text-lg">{p.content}</div></article>}
